;(function($){
	$.salvia.Class.namespace("$.TalkCenter.classes.mvc.view");
	$.TalkCenter.classes.mvc.view.newsView = new $.salvia.Class($.TalkCenter.classes.mvc.view.baseView,{
		init:function(){
			/*this.view = {};
			this.view.topNews = $("#topNews").css({
				left:$(window).width()/2-480,
				width:960
			}).show();
			this.view.topNews.find(".planes>div").click($.proxy(function(){this.request("TalkCenter","initExperInline")},this));
			this.view.topNews.scrollable({ items: ".planes",vertical:true,circular:true }).navigator().autoscroll({
				autoplay: true,
				autopause: true
			}); */
		}
	});
})(jQuery);